--It is possible to restore rows that have been deleted using the DELETE command.
--TRUE

--A DML command automatically issues a COMMIT.
--FALSE

--Oracle's multi-versioning feature achieves read-consistent query results.
--TRUE

--The TRUNCATE command and the TRUNC function can be used interchangeably.
--FALSE

--Uncommitted changes can be seen by all users.
--FALSE 

--You cannot selectively delete rows from a table.
--FALSE

--DML statements such as INSERT, UPDATE, DELETE, and MERGE obtain a lock on the row(s), so other users cannot manipulate it.
--TRUE

--A session is an individual connection to the Oracle database server.
--TRUE

--Transaction control determines when data manipulation becomes permanent in a database.
--TRUE 

--The TRUNCATE command removes all data permanently from a table.
--TRUE